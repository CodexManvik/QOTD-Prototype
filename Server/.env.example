# Server Config
PORT=3000
NODE_ENV=development

# Database Config
# Options: json, mongodb
DB_TYPE=json

# MongoDB URI (Required if DB_TYPE=mongodb)
# Format: mongodb://localhost:27017/qotd or MongoDB Atlas URI
MONGODB_URI=
